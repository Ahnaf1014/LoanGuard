# Architecture reference

The maintained architecture description is [the repository architecture guide](../ARCHITECTURE.md).

The canonical database definition is under `database/`; `backend/database/`
contains runtime connection code and does not contain schema definitions.
