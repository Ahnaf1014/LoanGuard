# Changelog

## 2026-08-09 — architecture consolidation

- Consolidated the backend into `core`, `database/repositories`, `services`,
  `web`, and feature-scoped templates.
- Removed inactive route, repository, template, and compatibility duplicates.
- Centralized service-level database error translation and refreshed project
  documentation and diagram organization.

## 2026-08-08 — engineering hardening

- Added a reproducible VS Code workspace, SQLTools connection, test/debug tasks,
  shared formatting rules, and extension recommendations.
- Added an application factory, CSRF protection, security headers, shared form
  validation, role-aware decisions, and deterministic database cleanup.
- Hardened MySQL constraints and workflow triggers; added backup, migration,
  health-check, integration-check, and web-smoke scripts.
- Removed tracked credentials and generated bytecode while preserving the local
  environment file.

## 2026-08-07 — context baseline

- Created repository-root memory files to capture the current architecture,
  schema, features, coding style, and recurring implementation risks.
- Recorded the intended end-to-end workflow from the supplied project overview:
  borrower → application → assessment → manager decision → loan → payment.
- Recorded the currently implemented UI boundary: dashboard, borrower,
  application, and assessment modules.
- Existing workspace changes were preserved; this memory does not imply that
  uncommitted application code has been reviewed, approved, or released.
