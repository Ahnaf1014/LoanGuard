"""Feature-specific Flask blueprints for the LoanGuard web interface.

Add a new user-facing capability as its own route module here, then register
its blueprint in ``backend/app.py``. This keeps unrelated features isolated.
"""
